name: Publish Docs

on:
  push:
    branches: [main]
    paths: ['fern/**']

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish Fern Docs
        uses: fern-api/docs-action@v1
        with:
          fern-token: ${{ secrets.FERN_TOKEN }}
