"""Streamlit webapp package for SUMMARIZE.

Modules are intentionally small and single-purpose so each concern
(theme, config, state, history, summarization pipeline, mermaid,
clipboard, UI) can be inspected or modified in isolation.
"""
